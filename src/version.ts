/**
 * The installed `macp-sdk-typescript` package version.
 *
 * Kept in sync with package.json by release-please (see the
 * `x-release-please-version` annotation).
 */
export const VERSION = '0.5.0'; // x-release-please-version
